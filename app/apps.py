# from django.apps import AppConfig

# class AppConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'app'  # <-- make sure this matches your app folder name exactly

#     def ready(self):
#         import app.signals  # <-- this loads your signals.py when Django boots
